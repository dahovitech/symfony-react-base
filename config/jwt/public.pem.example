# Exemple de clé publique JWT
# Cette clé publique est fournie à titre d'exemple pour le format attendu.
# Chaque développeur doit générer sa propre paire de clés.
#
# Pour générer les clés :
# 1. Générer la clé privée (RSA 4096 bits) :
#    openssl genrsa -out config/jwt/private.pem 4096
#
# 2. Générer la clé publique à partir de la clé privée :
#    openssl rsa -in config/jwt/private.pem -pubout -out config/jwt/public.pem
#
# 3. (Optionnel) Protéger la clé privée avec une phrase de passe :
#    openssl rsa -in config/jwt/private.pem -out config/jwt/private.pem.new -passout pass:VOTRE_PHRASE
#    mv config/jwt/private.pem.new config/jwt/private.pem
#
# 4. Définir les permissions appropriées :
#    chmod 600 config/jwt/private.pem
#    chmod 644 config/jwt/public.pem

-----BEGIN PUBLIC KEY-----
MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEA...
(Votre clé publique sera ici)
-----END PUBLIC KEY-----
